🛠 Стек технологий
Core: Python 3.x

Framework: Pytest

UI Tool: Playwright (Synchronous API)

Design Pattern: Page Object Model (POM)

CI/CD: GitHub Actions

🧪 Реализованные тесты и функционал
Проект покрывает критический путь пользователя (Critical Path) и включает:

Авторизация:

test_valid_login: Успешный вход под стандартным пользователем.

test_locked_out_user: Обработка блокировки аккаунта.

Работа с корзиной (Cart):

test_add_item_to_cart: Добавление товара и проверка UI-счетчика.

Data-Driven Testing:

test_multiple_items_addition: Параметризованный тест, который за один прогон проверяет добавление 3-х разных товаров (рюкзак, фонарик, футболка), используя динамические селекторы.

⚙️ Особенности архитектуры (То, что оценит Senior QA)
Page Object Model: Каждая страница сайта описана отдельным классом в папке pages/, что исключает дублирование кода и упрощает поддержку.

CI/CD Pipeline: Настроен автоматический запуск тестов в облаке при каждом push.

Smart Reporting: Реализован кастомный хук в conftest.py, который делает скриншот страницы в случае падения теста.

Artifacts: Скриншоты упавших тестов сохраняются в артефакты GitHub Actions для быстрого дебага.

Headless Mode: Тесты оптимизированы для запуска в изолированных CI-контейнерах.

🚀 Как запустить локально
Клонировать репозиторий:

Bash

git clone https://github.com/Emir120802/saucedemo-autotests.git
Установить зависимости:

Bash

pip install -r requirements.txt
Установить браузеры Playwright:

Bash

playwright install
Запустить все тесты:

Bash

pytest
